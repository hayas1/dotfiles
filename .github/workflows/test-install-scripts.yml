name: Test install scripts

on:
  push:
    branches: ["master"]
  pull_request:
    branches: ["master"]

env:
  INSTALL_SCRIPTS: ./public/build

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: install-python
        run: ${INSTALL_SCRIPTS}/install-python.sh && python --version && pip --version
